build:
  tests:
    override:
      - true
  nodes:
    tests:
      environment:
        php:
          version: 8.0

    analysis:
      environment:
        php:
          version: 8.0

      tests:
        override: [ php-scrutinizer-run ]